# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Local data cache & built DB (do NOT commit your private Strava data).
# Fixtures are deterministic (seeded) and regenerated on demand via
# `make fixtures` / conftest, so they are generated, not committed.
/data/

# Claude Code local state / agent worktrees
.claude/

# OS / editor
.DS_Store
.idea/
.vscode/
